/**
 * Shared copy shapes for typed components (subset of full i18n tree).
 */
export type VideoCopy = {
  tag: string;
  titleBefore: string;
  titleEm: string;
  titleAfter: string;
  lede: string;
  placeholderCta: string;
};
